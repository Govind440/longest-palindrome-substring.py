# All python keywords are reserved words in python which have
# special meaning to the interpreter and cannot be used as identifiers
# (variable names, function names, etc.) in your code.
# Here is a list of all the keywords in Python 3.10:
# False      await      else       import     pass
# None       break      except     in         raise
# True       class      finally    is         return
# and        continue   for        lambda     try
# as         def        from       nonlocal   while
# assert     del        global     not        with
# yield
j = 10
print(j)
